{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://solidping.io/schemas/checks/ssl.json",
  "properties": {
    "host": {
      "type": "string"
    },
    "port": {
      "type": "integer"
    },
    "warningDays": {
      "type": "integer"
    },
    "criticalDays": {
      "type": "integer"
    },
    "thresholdDays": {
      "type": "integer"
    },
    "timeout": {
      "type": "string",
      "x-solidping-go-type": "time.Duration"
    },
    "serverName": {
      "type": "string"
    }
  },
  "type": "object",
  "required": [
    "host"
  ],
  "title": "ssl check config",
  "description": "Check SSL certificate validity.\n\nDESCRIPTIVE ONLY. This schema exists for editor completion and third-party tooling; it is NOT the validator. SolidPing validates a check config with the Go `Validate()` of its checker (run it yourself with `sp checks validate`), which enforces rules this schema does not and cannot express. A config that satisfies this schema may still be rejected.\n\n`criticalDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.\n\n`port` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.\n\n`serverName` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.\n\n`thresholdDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.\n\n`warningDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.",
  "x-solidping-check-type": "ssl",
  "x-solidping-notes": [
    "`criticalDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.",
    "`port` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.",
    "`serverName` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.",
    "`thresholdDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative.",
    "`warningDays` has no `omitempty` in Go, but `Validate()` accepts a config without it, so it is NOT listed in `required`. The Go validator is authoritative."
  ],
  "x-solidping-secret-fields": [],
  "x-solidping-validation": {
    "authority": "go",
    "command": "sp checks validate <manifest>",
    "note": "DESCRIPTIVE ONLY. This schema exists for editor completion and third-party tooling; it is NOT the validator. SolidPing validates a check config with the Go `Validate()` of its checker (run it yourself with `sp checks validate`), which enforces rules this schema does not and cannot express. A config that satisfies this schema may still be rejected."
  }
}
