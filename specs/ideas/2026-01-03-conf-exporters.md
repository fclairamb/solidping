People might be worried that the service might disappear and they want to be able to export their configuration,
to do that we support exporting the configuration.

This API is available to admin users only.

/api/v1/orgs/{org_id}/checks/export

It shall generate one JSON file with all the checks and their configuration.
