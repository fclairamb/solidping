---
sidebar_position: 2
title: Tour
description: See SolidPing in action — a recorded walkthrough of creating your first HTTP check.
---

import useBaseUrl from '@docusaurus/useBaseUrl';

# Tour

Before installing anything, here is what SolidPing actually looks like.

Everything below is **recorded from the real dashboard**, not mocked up: the
video and screenshots are generated by driving the live UI with Playwright
(`make showcase`), so they get regenerated whenever the dashboard changes
instead of quietly going stale.

## Creating an HTTP check

The canonical flow, end to end: the checks list → **New check** → pick the
HTTP check type → fill in the target URL and a name → choose how often it runs
→ save, and land on the check's detail page as the first results arrive.

The camera pushes in on whatever is being filled in and pulls back out for
every navigation — that framing is added in post-production from cue points the
recording writes, so the dashboard you see is the shipped UI at its real size,
never a zoomed browser.

{/* Two encodes of the same cut: AV1 first because it is roughly half the size,
    H.264 behind it because Safari only decodes AV1 on Apple-silicon machines
    with the hardware decoder. The browser takes the first <source> it can play,
    so nobody lands on the fallback text. */}
<video
  controls
  loop
  muted
  playsInline
  style={{ width: '100%', maxWidth: '1280px', borderRadius: '8px' }}
>
  <source
    src={useBaseUrl('/showcase/create-http-check.mp4')}
    type='video/mp4; codecs="av01.0.05M.08"'
  />
  <source
    src={useBaseUrl('/showcase/create-http-check.h264.mp4')}
    type='video/mp4; codecs="avc1.640028"'
  />
  Your browser cannot play this video.
</video>

## The checks list

Every check you monitor, with its current status at a glance. Checks can be
grouped, filtered, and organised by host.

<img
  src={useBaseUrl('/showcase/01-checks-list.png')}
  alt="The SolidPing checks list for an organisation called Northwind Systems, with three HTTP checks listed by name, type, target URL, status and last response time"
  style={{ width: '100%', maxWidth: '1280px', borderRadius: '8px' }}
/>

## The new-check form

One form for every protocol. Pick the check type and the form reshapes itself
around it — method, URL and expected status codes for HTTP, host and port for
TCP, and so on. Scheduling — how often the check runs — sits just below, as the
video above shows. Once you run more than one region, a region picker appears
alongside it so you can choose where the check runs from.

<img
  src={useBaseUrl('/showcase/02-check-form-filled.png')}
  alt="The new-check form with the HTTP type selected, a GET request to https://api.solidping.io/v1/health, an expected status of 200, the name Production API, and a check interval of one minute"
  style={{ width: '100%', maxWidth: '1280px', borderRadius: '8px' }}
/>

## The check detail page

Once saved, the check starts running immediately. Its detail page shows the
live status, response times over time (per region), and the raw result history.

<img
  src={useBaseUrl('/showcase/03-check-detail.png')}
  alt="The Production API check's detail page: status, last-checked and open-incident tiles above a response-time chart, moments after the check was created"
  style={{ width: '100%', maxWidth: '1280px', borderRadius: '8px' }}
/>

## Next steps

- [Install SolidPing](/installation/docker) and create your first check
- Browse the [check types](/features/check-types) SolidPing supports
- Read the [configuration reference](/configuration)
