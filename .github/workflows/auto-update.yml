name: Auto-update PRs

on:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  auto-update:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: castastrophe/actions-pr-auto-update@v3.0.1
        with:
          token: ${{ secrets.CI_PAT }}
          include_labels: autoupdate, automerge
          limit: 2
