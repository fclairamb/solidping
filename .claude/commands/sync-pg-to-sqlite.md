Ensure SQLite matches PostgreSQL implementation.

**Verify**:
1. Migration files have SQLite equivalents
2. SQL queries work on both databases
3. Data types are compatible
4. Tests run on both backends
